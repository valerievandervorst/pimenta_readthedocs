[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "pimenta"
authors = [{name = "Valeire van der Vorst", email = "valerie.vandervorst@wur.nl"}]
dynamic = ["version", "description"]
